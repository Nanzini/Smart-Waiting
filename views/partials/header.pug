if users.id === false  
    .header_column1
        a(href=routes.home) 
            img(src="/uploads/SmartLogo.png" style={ width:"100px", height:"50px"})
    .header_column2
        a(href=routes.join) 회원가입
        a(href=routes.login) 로그인
            

else
    .header_column1
        a(href=routes.home) Home
    .header_column2
        p #{users.email}
        a(href=routes.userInfo(users.id)) 내정보
        a(href=routes.logout) 로그아웃