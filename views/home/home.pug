extends ../partials/main.pug

block content    
        if users.id !== false 
            a(href=routes.register)
                h1 가게등록
            
            a(href=routes.posHome)
                h1 종업원 모드
    
        else
            h1 Just Home
            
        a(class='go_reserve' href=routes.reserve)
            i(class='fas fa-pen-square fa-8x')
            h1 예약하기
